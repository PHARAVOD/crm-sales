\# Отчет по оптимизации производительности



\## Таблица замеров (ДО оптимизации)



| Операция | Время (мс) | Время (сек) | Примечание |

|----------|------------|-------------|------------|

| Загрузка каталога (1 запрос) | 450 мс | 0.45 сек | Без кэша |

| Загрузка каталога (10 запросов) | 4500 мс | 4.5 сек | Каждый раз запрос в БД |

| Добавление в корзину (1 запрос) | 120 мс | 0.12 сек | Синхронно |

| Добавление в корзину (10 запросов) | 1200 мс | 1.2 сек | Последовательные вызовы |

| \*\*Полный процесс заказа\*\* | \*\*2300 мс\*\* | \*\*2.3 сек\*\* | \*\*Главный сценарий\*\* |



\## Узкие места (профилирование)



| Компонент | Время | % от общего | Проблема |

|-----------|-------|-------------|----------|

| Модуль оплаты | 1500 мс | 65% | Синхронный вызов API |

| Проверка остатков | 300 мс | 13% | Последовательные запросы |

| Загрузка каталога | 450 мс | 20% | Нет кэша |

| Сохранение в БД | 50 мс | 2% | OK |



\## Оптимизации



\### 1. Кэширование каталога (Redis)



\*\*ДО:\*\* 450 мс на запрос

\*\*ПОСЛЕ:\*\* 50 мс на запрос (после первого)



```csharp

public async Task<List<Product>> GetProducts()

{

&#x20;   var cached = await \_cache.GetStringAsync("products");

&#x20;   if (cached != null)

&#x20;       return JsonSerializer.Deserialize<List<Product>>(cached);

&#x20;   

&#x20;   var products = await \_context.Products.ToListAsync();

&#x20;   await \_cache.SetStringAsync("products", JsonSerializer.Serialize(products),

&#x20;       new DistributedCacheEntryOptions 

&#x20;       { 

&#x20;           AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5) 

&#x20;       });

&#x20;   return products;

}

